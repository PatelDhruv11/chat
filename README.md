# Python AI App Example

This repository contains one simple AI app built with Python:

- `app.py`: a mini command-line sentiment classifier using a tiny Naive Bayes model.

## Run

```bash
python3 app.py
```

Then type a sentence like:

- `I really love this`
- `This is awful`

Type `quit` to exit.
